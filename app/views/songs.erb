<h1>Song Titles</h1>

<% @songs.each do |song| %>
  <div class="song">
  <h3><a href="/songs/<%= song.id %>"><%= song.title %></a> <small>(by: <%= song.author %>)</small></h3>
    <p>
      <%= song.url %>
    </p>
  </div>
<% end %>

<hr/>
<p>Nice taste in tunes</p>